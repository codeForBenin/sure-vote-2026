vich_uploader:
    db_driver: orm
    mappings:
        vote_reports:
            uri_prefix: /uploads/pv
            upload_destination: '%kernel.project_dir%/public/uploads/pv'
            namer: vich_uploader.namer_uniqid
